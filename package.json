{
  "name": "goal-tracker-api",
  "version": "1.0.0",
  "description": "Short-term goal tracker based on the Self Journal",
  "scripts": {
    "local-start": "node app.js",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && node app.js",
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && cd client && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "basic-auth": "^2.0.1",
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "mongoose": "^6.0.6",
    "pug": "^3.0.2"
  },
  "author": "Karla Walker",
  "license": "MIT"
}
